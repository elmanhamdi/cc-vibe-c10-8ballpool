{
  "name": "vertical-eight-ball",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "validate:manifest": "node scripts/validate-asset-manifest.mjs",
    "validate:manifest:strict": "node scripts/validate-asset-manifest.mjs --strict",
    "build": "npm run validate:manifest && tsc && vite build",
    "build:docs": "npm run validate:manifest && tsc && vite build --outDir docs",
    "preview": "vite preview",
    "export:power-meter": "node scripts/gen-power-meter-png.mjs"
  },
  "dependencies": {
    "three": "^0.170.0"
  },
  "devDependencies": {
    "@types/three": "^0.170.0",
    "sharp": "^0.34.5",
    "typescript": "^5.7.0",
    "vite": "^6.0.0"
  }
}
